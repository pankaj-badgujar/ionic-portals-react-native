import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RNPortals',
  webDir: 'web/RNPortals/public',
  bundledWebRuntime: false
};

export default config;
